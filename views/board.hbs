<div class="page-heading-board d-flex justify-content-between">
    <h2>{{board_name}}</h2>
    <button type="button" class="btn-add-col">New column</button>
</div>
<div class="board-body d-flex justify-content-between" total_columns={{total_columns}}>
    {{#each columns}}
    <div class="col-retro" total_columns={{total_columns}}>
        <div class="d-flex justify-content-between">
            <div class="col-name">{{this.column_name}}</div>
            <i class="fas fa-trash icon-trash"></i>
        </div>
        <button type="button" class="btn-add-task">+</button>

        <div class="drag-task" id="drag-item-{{@key}}">
            <div class="no-drag">.</div>
            {{#each this.task}}
            <div class="task-retro">
                <div class="d-flex justify-content-between">
                    <div class="task-name">{{this.task_name}}</div>
                    <i class="fas fa-pen"></i>
                </div>
                <div class="d-flex justify-content-between ">
                    <div class="task-owner">{{this.task_owner}}</div>
                    <div class="icon-feedback pr-3">
                    <i class="fas fa-thumbs-up mr-3"> {{this.total_vote}}</i>
                    <i class='fas fa-comment-alt'> {{this.total_comment}}</i>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>

    </div>
    {{/each}}
</div>